const envConfig = {
  baseUrl: String(import.meta.env.VITE_BASE_URL),
  // : String(import.meta.env.),
  // : String(import.meta.env.),
  // : String(import.meta.env.),
  // : String(import.meta.env.),
};

export default envConfig;
